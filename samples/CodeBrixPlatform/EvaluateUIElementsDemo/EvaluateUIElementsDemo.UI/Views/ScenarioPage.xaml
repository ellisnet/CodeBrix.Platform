<Page x:Class="EvaluateUIElementsDemo.Views.ScenarioPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      FontFamily="{StaticResource OpenSansFont}">

  <Grid Padding="16" RowSpacing="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16">
      <Button x:Name="BackButton" Content="&lt; Back" MinWidth="96" Click="BackButton_Click" />
      <TextBlock x:Name="TitleText" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
    </StackPanel>

    <TextBlock Grid.Row="1" x:Name="InstructionsText" TextWrapping="Wrap" MaxWidth="1000" HorizontalAlignment="Left" />

    <!-- The scenario's own view goes here. The page scrolls both ways so a wide or tall scenario is
         never clipped by the window. -->
    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollMode="Auto" VerticalScrollMode="Auto">
      <ContentPresenter x:Name="ScenarioHost" HorizontalAlignment="Left" VerticalAlignment="Top" />
    </ScrollViewer>
  </Grid>
</Page>
