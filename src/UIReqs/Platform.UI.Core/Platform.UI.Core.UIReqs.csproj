<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<AssemblyName>CodeBrix.Platform.UI.Core.UIReqs</AssemblyName>
		<RootNamespace>CodeBrix.Platform.UI.Core.UIReqs</RootNamespace>
	</PropertyGroup>

	<!-- The runner, the framework flavour, the packages, the fonts, the feature root and the
	     Skia-assembly swap: everything every UIReqs project is. This is the Landscape project of
	     the core pair, so it sets no $(UIReqsLandscapeProjectDir) - it IS one. -->
	<Import Project="..\UIReqs.Common.targets" />

</Project>
