namespace CodeBrix.Platform.UI.AddIn.Graphics2DSK.UIReqs.Support;

/// <summary>
/// The plain fixture element: it paints the stripes its Fill names across its whole drawing
/// area and nothing else. This is the element behind the noun "FillCanvas" in a feature file,
/// and it is what almost every Graphics2DSK scenario shows.
/// </summary>
public sealed class FillCanvas : SKCanvasFixture
{
}
