<Project Sdk="Microsoft.NET.Sdk">
	<!-- Assembly identity pinned to the legacy name; the rename step changed only the .csproj
	     filename. PackageId is NOT the assembly name: this project never packs itself
	     (IsPackable=false below - its package is built from build/nuget/Platform.WinUI.Graphics3DGL.nuspec),
	     so the only thing PackageId governs is how another project in this repository that takes a
	     ProjectReference to it resolves its package dependency. That has to be the PUBLISHED id,
	     the one the nuspec and build/nuget/package-dependency-map.json both name - otherwise a
	     referencing package (the VideoPlayer add-in, which needs SkiaGpuContext) would ship a
	     dependency on an id that has never been published. -->
	<PropertyGroup>
		<AssemblyName>CodeBrix.Platform.WinUI.Graphics3DGL</AssemblyName>
		<PackageId>CodeBrix.Platform.Graphics3DGL.ApacheLicenseForever</PackageId>
	</PropertyGroup>

    <PropertyGroup>
		<TargetFrameworks>$(NetSkiaPreviousAndCurrent)</TargetFrameworks>
		<TargetFrameworks Condition="'$(BuildGraphics3DGLForWindows)' != ''">$(TargetFrameworks);$(NetUWPOrWinUI)</TargetFrameworks>

		<RootNamespace>CodeBrix.Platform.WinUI.Graphics3DGL</RootNamespace>
		<!-- Packaging is nuspec-driven: build/nuget/Platform.WinUI.Graphics3DGL.nuspec packed by
		     build/CodeBrix.Platform.Build.csproj (as CodeBrix.Platform.Graphics3DGL.ApacheLicenseForever).
		     Do NOT self-pack this project — GeneratePackageOnBuild must stay off so the legacy
		     WinUI-named package id is never produced. -->
		<IsPackable>false</IsPackable>

		<Nullable>enable</Nullable>
		
		<!-- Required as the project name does not end with `.Skia` when building for netcoremobile targets -->
		<CodeBrixDisableTargetFrameworkPlatformOverride Condition="!$(CodeBrixTargetFrameworkOverride.Contains('windows10'))">true</CodeBrixDisableTargetFrameworkPlatformOverride>
    </PropertyGroup>

	<Import Project="../../targetframework-override.props" />

	<ItemGroup>
		<PackageReference Include="CodeBrix.Platform.OpenGL.MitLicenseForever" Version="1.0.209.135" />
		<!-- Version is pinned centrally by src/Directory.Build.targets (SkiaSharpVersion, currently
		     4.151.0); adding it here gives this add-in the GPU-Skia types (GRContext/GRGlInterface/
		     SKSurface) it needs for OffscreenGLContext.CreateGrContext() and SkiaGLCanvasElement. -->
		<PackageReference Include="SkiaSharp" />
	</ItemGroup>

	<ItemGroup Condition="!$(TargetFramework.Contains('windows10'))">
		<ProjectReference Include="..\..\Platform.UI\Platform.UI.Skia.csproj" PrivateAssets="all" />
	</ItemGroup>

	<Choose>
		<!-- Android -->
		<When Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)'))=='android'">
			<PropertyGroup>
				<DefineConstants>$(DefineConstants);ANDROID</DefineConstants>
			</PropertyGroup>
		</When>
		<!-- Skia Desktop -->
		<When Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)'))==''">
			<PropertyGroup>
				<DefineConstants>$(DefineConstants);CROSSRUNTIME</DefineConstants>
			</PropertyGroup>
		</When>
		<!-- WinUI -->
		<When Condition="$(TargetFramework.Contains('windows10'))">
			<PropertyGroup>
				<RuntimeIdentifiers>win-x86;win-x64;win-arm64</RuntimeIdentifiers>

				<DefineConstants Condition="'$(CODEBRIX_UWP_BUILD)'!='true'">$(DefineConstants);WINDOWS_UWP</DefineConstants>
				<DefineConstants Condition="'$(CODEBRIX_UWP_BUILD)'=='true'">$(DefineConstants);WINAPPSDK</DefineConstants>
				<DefineConstants>$(DefineConstants);WINAPPSDK</DefineConstants>
				<UseRidGraph>true</UseRidGraph>
				<WindowsPackageType>None</WindowsPackageType>
			</PropertyGroup>
			<ItemGroup>
				<PackageReference Include="Microsoft.WindowsAppSDK" Version="2.4.0" Condition="'$(CODEBRIX_UWP_BUILD)'!='true'" />
				<PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.28000.1839" PrivateAssets="all" Condition="'$(CODEBRIX_UWP_BUILD)'!='true'" />

				<Compile Include="..\..\Platform.UI.Runtime.Skia.Wpf\Rendering\WindowsRenderingNativeMethods.cs" />
				<Compile Include="..\..\Platform.UI.Runtime.Skia.Wpf\Extensions\WpfNativeOpenGLWrapper.cs" />
				<Compile Include="..\..\Platform.UI\Graphics\INativeOpenGLWrapper.cs" />
			</ItemGroup>
		</When>
	</Choose>

	<PropertyGroup>
		<DefineConstants Condition="$(CODEBRIX_UWP_BUILD) == true">$(DefineConstants);CODEBRIX_UWP_BUILD</DefineConstants>
	</PropertyGroup>

	<PropertyGroup>
		<DefaultItemExcludes>$(DefaultItemExcludes);angle/**</DefaultItemExcludes>
	</PropertyGroup>

	<!-- The macOS ANGLE dylibs under angle_binaries/osx are VENDORED IN THIS REPOSITORY
	     (BSD 3-Clause; THIRD-PARTY-NOTICES Part E, item 22) so package builds never depend
	     on an external download. They were originally the unmodified builds published by
	     the Silk.NET project (Silk.NET repo commit a9c75a6, Silk.NET.OpenGLES.ANGLE.Native,
	     runtimes/osx/native). Do not add download or clean steps for them. -->

	<Target Name="VerifyAngleBinaries" BeforeTargets="Build">
		<Error Condition="!exists('$(MSBuildThisFileDirectory)angle_binaries/osx/libEGL.dylib') or !exists('$(MSBuildThisFileDirectory)angle_binaries/osx/libGLESv2.dylib')"
		       Text="The vendored ANGLE dylibs are missing from angle_binaries/osx — they are committed repo files; restore them from git." />
	</Target>

	<ItemGroup>
		<None Include="..\..\..\icon-codebrix-128.png" Pack="true" PackagePath="\" />
		<None Include="..\..\..\README.md" Pack="true" PackagePath="\" />
		<None Include="AGENT-README.txt" Pack="true" PackagePath="\" />
		<None Include="..\..\..\THIRD-PARTY-NOTICES.txt" Pack="true" PackagePath="\" />
		<Content Include="Assets\**\*.*" CopyToOutputDirectory="PreserveNewest" />
	</ItemGroup>
	
	<ItemGroup>
		<None Include="$(MSBuildThisFileDirectory)angle_binaries/osx/lib*" PackagePath="runtimes/osx/native" Pack="true" Visible="False" />
	</ItemGroup>
	<ItemGroup>
		<ProjectReference Include="../../CodeBrix.Platform.Extensions.Logging/CodeBrix.Platform.Extensions.Logging.csproj" />
	</ItemGroup>
</Project>
