<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
  <!-- The light half of a two-theme icon set. Its only job here is to have a twin whose manifest
       name ends the same way, so the suffix form of a cb-res:// URI is ambiguous. -->
  <circle cx="10" cy="10" r="7" fill="none" stroke="currentColor" stroke-width="2" />
</svg>
